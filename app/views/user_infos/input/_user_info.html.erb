<% # entity of user_info selection for new and edit form template -%>
<% # locals 
   #   prefix  : prefix for association of user_infos
   #   assoc_name : name of association
   #   options :  :aff_root, :sta_root => each root, out_of_selection_for_aff & sta
%>

<%  options[:assoc_target_body_template] ||= "#{options[:collection_name]}/input/assoc_target_body" %>

<%= render_assoc_target_to_put user_info, prefix, assoc_name, options %>