<% # entity of organization selection for new and edit form template -%>
<% # locals 
   #   prefix  : prefix for association of arganizations
   #   assoc_name : name of association
   #   options :  :aff_root, :sta_root => each root, out_of_selection_for_aff & sta
%>

<%= render_assoc_target_to_put organization, prefix, assoc_name, options %>