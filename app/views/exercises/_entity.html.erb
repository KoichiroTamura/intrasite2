<% # entity for exercise %>

<% # for an entity of exercise %>


<tr class="pointer <%= odd_or_even_row(entity_counter)%>" <%= remote_call_for_detail(entity) %>>
	<%= render_entity_items(entity) %>
</tr>
